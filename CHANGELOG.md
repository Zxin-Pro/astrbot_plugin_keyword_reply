# 更新日志

## v2.2.2 (2026-09-17)

- 修复：配置页上传的图片无法加载——面板实际将文件存储在 `data/plugin_data/<插件名>/files/`，现已支持多候选目录解析（中文名目录 / 英文目录名 / 全目录扫描兜底 / 插件代码目录）
- 上传图片路径增加安全校验（`files/` 前缀 + 防路径穿越）

## v2.2.1 (2026-09-17)

- 修复：`image` / `text_image` 类型规则只上传图片、未填图片地址时，规则被误判跳过

## v2.2.0 (2026-09-17)

- 新增：规则卡片支持在配置页直接上传本地图片（png/jpg/jpeg/gif/webp/bmp），优先于手动填写的图片地址
- 图片地址与上传图片二选一，上传文件缺失时自动回退

## v2.1.0 (2026-09-17)

- 配置页升级：rules 改为 template_list 卡片式，每条规则单独一张卡片，可视化填写关键词 / 匹配方式（下拉）/ 回复类型（下拉）/ 回复文本 / 图片 / 生效群号 / 排除词 / 启用开关，无需手写 JSON
- metadata.yaml name 改为中文名「群聊关键词监控」

## v2.0.0 (2026-09-17)

- 重构为「群聊关键词监控」：仅监听群聊消息（GROUP_MESSAGE），私聊不处理
- 规则字段：keyword / match_type(contains·regex) / reply_type(text·image·text_image) / reply_text / reply_image / groups / enabled
- 新增：规则级生效群过滤、启用/禁用开关、排除词（消息含排除词不触发）
- 新增：群聊黑名单、用户白名单（非空=仅名单内用户触发）
- 新增：一条消息命中多条规则时可配置全部触发，规则间加延迟防风控
- rules 默认空列表，不预置任何示例规则；rules 为空或插件禁用时静默运行
- 正则启动时预编译，非法规则自动跳过并记日志；配置变更无需重启自动生效

## v1.0.0 (2026-09-17)

- 首个版本：群聊关键词自动回复（文本/图片/图文混合，模糊/精确/正则匹配，冷却、黑白名单）
